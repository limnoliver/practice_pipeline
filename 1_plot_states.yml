target_default: 1_plot_states

packages:
  - dplyr

sources:
  - 1_plot_states/code/get_state_data.R

targets:
  1_plot_states:
    depends:
      - minnesota_data
      
  minnesota_data:
    command: get_state_data(my_state = I('minnesota'))